interface:
  display_name: "Owner Deploy Trigger"
  short_description: "Owner empty-commit deploy for Vercel Hobby"
  default_prompt: "Use $setup-owner-deploy-trigger to add an owner empty-commit deploy workflow (OWNER_GIT_PAT) for Vercel Hobby pusher attribution."
